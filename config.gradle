ext {
    app = [
            // 编译
            compileSdkVersion: 30,
            buildToolsVersion: "30.0.3",
            // SDK指向版本
            targetSdkVersion : 30,
            // 支持最低版本
            minSdkVersion    : 16,
    ]

}